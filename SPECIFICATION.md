# やること・メモ スワイプアプリ 実装仕様

正文はユーザー提供の `Codex_Implementation_Specification.md`（2026-08-01）とする。本リポジトリはその仕様に基づき、Kotlin Multiplatform、Compose Multiplatform、Room KMP を採用する。

実装判断の優先順位は次のとおり。

1. 分類せずすぐ保存できること
2. やることとメモを別画面で扱うこと
3. 左右で処理、上下で前後移動できること
4. 時刻、優先度、必須前提の順序制約をDomain層で保証すること
5. 後回し回数に応じて小さく分ける提案を行うこと
6. ユーザーが配色、文字、形、余白、影、動きを編集できること
7. オフラインで完結し、操作を取り消せること

受け入れ条件ID（AC-ADD、AC-GES、AC-ORD、AC-SPL、AC-THM）と詳細値は、添付された正文を参照する。

