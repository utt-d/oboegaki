# やること・メモ スワイプアプリ 実装仕様

正文はユーザー提供の `Codex_Implementation_Specification.md`（2026-08-01）とする。本リポジトリはその仕様に基づき、Kotlin Multiplatform、Compose Multiplatform、Room KMP を採用する。

実装判断の優先順位は次のとおり。

1. 分類せずすぐ保存できること
2. やることとメモを別画面で扱うこと
3. 左右で処理、上下で前後移動できること
4. 時刻、優先度、必須前提の順序制約をDomain層で保証すること
5. 後回し回数に応じて小さく分ける提案を行うこと
6. ユーザーが配色、フォント、アイコン、文字倍率、形、余白、影、動きを編集できること
7. オフラインで完結し、操作を取り消せること
8. 新規インストール時は項目を自動生成せず、初回操作ガイドを表示すること
9. 操作ガイドを設定画面からいつでも再表示できること
10. やることとメモは種類別に、階層数を制限せずグループ化できること
11. 自分自身または子孫への移動、種類の異なるグループへの移動をDomain層で拒否すること
12. やることとやることグループに日・週・月・年単位の定期設定を行えること
13. 定期設定には行う日時を必須とし、完了時にローカル時刻を保って次回分を生成すること
14. 定期グループは中の全階層と内部の必須前後関係をまとめて次回へ複製すること
15. Android公開版はリポジトリ外の継続的なリリース鍵で署名し、プライバシー方針と検証用ハッシュを公開すること

## グループ化と定期設定の追加受け入れ条件

- `AC-GRP-01`: 1,000階層のモデルを再帰呼び出しなしで列挙できる。
- `AC-GRP-02`: グループを折りたたむと全子孫を隠し、ルート項目として再表示しない。
- `AC-GRP-03`: 並べ替えは同じ親グループの兄弟間だけで行い、やることの時刻・優先度・必須前後関係を破らない。
- `AC-GRP-04`: グループ削除時は直下の子を一つ上の親へ移し、項目を失わない。
- `AC-REC-01`: 夏時間の切り替わりを跨いでも、日／週単位の定期予定は現地の同じ時刻を維持する。
- `AC-REC-02`: 月末に存在しない日付はその月の最終日に補正する。
- `AC-REC-03`: 終了日より後の次回項目を作成しない。
- `AC-REC-04`: 定期グループの完了は一つのトランザクションで、現行ツリーの完了と次回ツリーの生成を行う。

受け入れ条件ID（AC-ADD、AC-GES、AC-ORD、AC-SPL、AC-THM）と詳細値は、添付された正文を参照する。
