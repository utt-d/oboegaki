# 公開と配布

## Android

無料で公開する現在の方法は、リリース署名済みAPK、SHA-256、署名証明書フィンガープリント、ソースコード、プライバシーポリシーをGitHub Releasesと配布ページへ掲載する方法です。APKは同じリリース鍵で継続して署名します。

Google Playは一度だけ25米ドルの登録料が必要なため「完全に無料」ではありません。F-Droidは無料ですが、公開ソースから再現可能にビルドできる状態と審査が必要です。本リポジトリはMIT Licenseかつ追跡・広告・独自SDKなしなので、今後F-Droidのメタデータを提出できる構成です。

Androidの開発者確認制度は段階的に導入されています。2026年9月の要件は参加ストア経由の配布が中心ですが、2027年以降の展開を見据え、配布方法を定期的に再確認してください。無料の限定配布枠は20端末までで、広い配布を継続する場合は本人確認を伴う登録が必要になる見込みです。

### 初回の署名鍵作成

署名鍵は `C:\Users\utt55\Desktop\oboegaki\release-signing\oboegaki-release.jks` に置き、リポジトリへ含めません。パスワードは十分な長さの乱数を使い、Windows DPAPIで `release-password.dpapi` に暗号化します。秘密鍵と暗号化パスワードは、別の暗号化媒体にもバックアップしてください。紛失すると同じアプリとして更新できません。

通常のリリースビルドは次で行います。

```powershell
./tools/build-release.ps1
```

### 旧デバッグ版からの移行

0.1.6以前の公開APKはデバッグ鍵で署名されていました。安全なリリース鍵へ切り替えた0.2.0は上書き更新できません。旧版でバックアップを書き出し、旧版をアンインストールして0.2.0を入れ、バックアップを読み込む必要があります。0.2.0以降は同じリリース鍵を維持します。

## iOS

App StoreとTestFlightによる一般公開にはApple Developer Program（年額99米ドル）が必要で、恒久的な無料公開方法はありません。無料Apple Accountは自分の端末での開発テスト用で、署名の有効期間と端末数に制限があります。

このため現時点ではiOSのソースを公開し、利用者自身がmacOS、Xcode、Apple Accountでビルドする方法のみ無料で提供します。第三者配布を装った証明書共有や、規約に反する署名サービスは使用しません。App Store公開は登録費用と審査を受け入れる時点で行います。
